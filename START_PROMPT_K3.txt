【Kimi-K3 开工提示词 · P0+旅程叙事轮+P1 批次 · 2026-08-20 授权 · v1.0】

你是本仓库（COMAC Agent Hub）的实现工程师。本提示词授权一个有界批次：P0（8/30 前最小集）+ 旅程叙事轮 + P1 口吻轮。三个子批之外的任何能力（P2 组织侧 / P3 评委侧 / P4 外部集成）均未授权，不得开工。

〇、第一步（按序读完再动手）
1. `AGENTS.md`（仓库宪法，最高优先级）；
2. `IMPLEMENTATION_PLAN.md` §28–§30（第四幕现状、打磨轮⑥、本批次提案与批次映射）；
3. `docs/product/07`（0820 PRD 对齐）、`docs/product/08`（四节点旅程与场景切换语法——本批设计规范）、`docs/product/09`（D5–D7 细则，本批仅作背景，不实施）；
4. `docs/product/02`（Coach 人格规范——P1 要修订它）；
5. `docs/product/01`（UI/UX 基线）。
开工前审计：当前分支 `agent/hub-deep-ux-image2-polish` 上有未提交的文档改动（docs/product/07、08、09，IMPLEMENTATION_PLAN §30，AGENTS.md 一处完成态表述，以及本文件）。先把它们按主题提交（建议一笔「文档:0820 PRD 对齐与四节点旅程拍板落盘」），再开工；绝不 reset、不覆盖无关工作。未经明确要求不推送远程、不部署、不重启生产服务。

一、仓库现状（30 秒版）
- 双产品共存：`app/(hub)` 新 Hub——固定视口三幕 Coach + 第四幕问题定义 Artifact，一问一幕，会话级、无 DB；`lib/hub/coach-machine.ts` 纯函数状态机；`POST /api/hub/coach` 是唯一 live 链路（GLM 受控适配、严格三字段输出、限流/日预算/超时确定性回退）。`app/(app)` 旧侧 MVP（账号/评委/组织者/WorkBuddy 总控，prisma+SQLite）——本批除 P0-3 走查外不碰旧侧。
- 质量基线：218 单测 / 98 e2e 全绿，Axe 零豁免，1280×800 与 375/390 零页面溢出。你的改动不得让任何一条变红。

二、已拍板决策（D1–D9，2026-08-20，不得重开、不得再问）
D1 三字段第二段保持「证据」；D2 Coach 口吻切「懂 Agent 落地的业务专家」（本批 P1 执行）；D3 可追述定向完整落库（P2 实施），本批只做导出内嵌过渡解；D4 隐私互不可见走查（本批仅审计）；D5–D7 双轨评分/评委权限/答疑卡细则见 docs/product/09（P2–P4 实施，本批不碰）；D8 智能体只组织+审阅参赛者自带测试证据，平台不代跑；D9 完成态 = 完整交付包 + 全过程证据，非任何完成度百分比。

三、本批工作项

【P0】
- P0-1 导出可追述过渡解：`composeSeedText`/`composeArtifactText` 内嵌生成时间（本地时钟）、六轮问答映射（主张←第1·3幕 / 影响←第2幕 / 深化←第4–6轮）、会话随机卡号（如 `QD-XXXXX`，注明「本会话生成，未落库」）、格式版本 v1。单测 + e2e 断言同步。
- P0-2 `/guide` 三件套引导：下载 WorkBuddy → 加入项目群 → 进入网站；链接/日期未定一律显示「待活动配置确认」，禁止编造。
- P0-3 隐私走查：旧侧参与端全部列表页过一遍「互不可见」（重点 inspirations 等可能暴露他队内容的页面），仅产出审计记录写入 IMPLEMENTATION_PLAN 新小节，修复留 P1。
- P0-4 PDF 手册初稿：站外交付物，放 `docs/manual/`，不进代码路径。

【旅程叙事轮（设计规范 = docs/product/08 §3–§4 五拍语法，严格遵循）】
- J-1 建立拍：第一幕尚无任何回答时的前置场景（coach-machine 新 phase）：三件套三步（pending 事实如实标注）+ 本页流程（6 问、约 10–15 分钟、产出一张可带走的卡）+ 隐私披露前置 +「开始第一问」。不是弹窗轮播；一屏一焦点；键盘全路径；reduce-motion 降级；此拍出现时顶栏「返回活动指南」逻辑不得回退。
- J-2 N1 终章交棒：问题定义卡完成态收尾区：已复制带走（真实状态）→ 粘贴到项目群/共享文件夹 →「中期答疑（第 2 周〔待活动配置确认〕）开放后凭此卡继续」→ 查看完整参与路径。
- J-3 `/guide` 操作层：四节点旅程（G0 到场 + N1 进行中 + N2/N3/N4「第 N 周开放·待确认」）；现有五段保留为方法论层；不新增路由；不做假状态、不预支未开放节点能力。
- J-4 跨表面词汇统一：G0–N4 命名贯穿 hub 与 guide（三字段名称按 D1 保持「证据」）。
- J-5 揭示拍溯源编排：种子/问题定义卡揭示时六轮回答摘录依次落位到对应槽（一次编排、reduce-motion 分支），诚实注记沿用「来自本次会话回答的摘录，不构成已验证的证据」。

【P1】
- P1-1 口吻轮（D2）：`docs/product/02` 升版——口吻基线从「AI 导师」改为「懂 Agent 落地的业务专家」：业务拷问优先（具体的人与损失），Agent 必要性拷问（「直接用 ChatGPT 不行吗」式）自然带出；严苛但不羞辱、不奉承。系统提示词措辞与 fixtures 判断/风险/问题话术随之校准；三字段输出合同（50–150 字）不动；e2e 人格断言同步改写。

四、红线（触碰即停，写了也要回滚）
1. 不做 P2–P4 任何能力：持久化/落库、评委端改造、组织侧流转、答疑卡、工单、WorkBuddy 集成、代码仓库 checkout、双轨评分 UI、部门字段。
2. 不改：三字段输出合同、`coach-provider` 校验、限流/日预算、既有路由路径。
3. 不改 `AGENTS.md` 与 `docs/product/07/08/09` 及 §30 的决策记录；实现中发现文档与代码矛盾时停下报告，不要自行改决策。
4. 活动事实（日期/链接/部门/奖项/规则）未知一律 pending 展示；不新增排行榜/健康分/完成率。
5. 固定视口、一屏一问一决策；动效必须解释状态变化且支持 prefers-reduced-motion；Axe 零豁免不新增。
6. 新能力只加在 `app/(hub)`、`components/hub/`、`lib/hub/`，不混入旧侧组件。

五、工程纪律
- 结构归状态机：建立拍/交棒拍是 `coach-machine` 的新 phase/reducer 分支 + 纯函数（含单测），UI 复用 CoachWorkspaceScene 体系。
- 增量提交、分主题（P0 一笔、旅程叙事轮一笔或多笔、P1 一笔），提交信息沿用仓库风格。
- e2e 全部在 mock 下跑（playwright webServer 已内嵌 `LLM_MOCK_MODE=true`，裸 `npx playwright test` 安全）；若改动判断/风险停留时长或幕间过渡时序，须同步全部相关 e2e 超时。
- live 链路验证只用 `npm run probe:coach`（真实出站、日预算内少量），绝不在 e2e 里打真实模型。
- 计划先行：动工前在 `IMPLEMENTATION_PLAN.md` 追加 §31（标题注明「P0+旅程叙事轮+P1 批次」），列实现决定与验收口径，然后直接实现，不停下来等例行确认。

六、完成定义（缺一不可，全部如实报告，没跑过的检查不得声称通过）
1. `npm run lint` / `npm run typecheck` 通过；
2. `npm run test` 全绿（新增用例列清单）；
3. `npm run build` 通过（含活动配置校验）；
4. `npx playwright test` 全绿（至少新增：建立拍全路径与键盘路径、终章交棒内容、guide 四节点操作层、导出可追述字段断言、口吻人格断言改写、新场景 Axe 零违规、375 零溢出）；
5. 演示录屏：用 `demo-record.mjs` 重录一版「建立拍 → 六问 → 问题定义卡 → 终章交棒」端到端（mock 模式即可），归档 `demo-recordings/`；
6. 最终报告：各检查实际结果、新增/修改文件清单、未完成项与原因、实现中发现的文档-代码矛盾。
只有真正阻塞实现的外部信息才向用户提问；其余一律采用可逆、配置化默认并把决策记入 §31。
