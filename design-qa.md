# 固定视口 Coach 工作台视觉对照审查

## 对照输入与归一化

- 视觉语言真值：`/Users/Zhuanz/.codex/generated_images/01a00bc8-f169-7bc0-910e-649894d3960d/exec-e02a8a10-e8a8-4715-8771-01a8d4244023.png`，916 × 1716，1× 像素密度。
- 用户最新布局真值：页面本身不得上下翻动；只有会话区域可纵向滚动；活动背景信息退出主入口；3D 核心视觉改为高质量平面资产。
- 归一化源图：`docs/screenshots/fixed-workbench/source-top-916.png`，从视觉真值顶部以 916 × 800 原尺寸裁切，没有非等比缩放。
- 浏览器实现：`docs/screenshots/fixed-workbench/home-916.png`，CSS 视口 916 × 800，1× 像素密度，`/` 默认 problem 入口第一幕。
- 同尺寸并排证据：`docs/screenshots/fixed-workbench/qa-source-vs-workbench-916.png`，1832 × 800；左侧源图，右侧实现。
- 额外实现证据：`docs/screenshots/fixed-workbench/home-1440.png`（1440 × 900）与 `home-390.png`（390 × 844），均为视口截图而非 full-page 拼接。

参考图负责艺术语言，用户文字负责产品构图。两者原本不是同一信息状态，因此没有把长卷段落数量当成像素匹配目标；暖白纸张、海军蓝／钴蓝、宋体标题、航空图谱细线与平面主视觉仍按同宽 1:1 图像比较。

## Findings

- 没有剩余 P0、P1 或 P2。
- [P3] 五张 RGBA 状态资产目前各约 1–2 MB。
  - Location：`public/hub/art/coach-state-*.png`。
  - Evidence：自然分辨率 1254 × 1254，透明边缘和纸张颗粒清晰；当前本地首屏无模糊或透明光晕，但网络传输仍可进一步收紧。
  - Impact：不影响本地体验和当前验收；正式公网首屏可继续降低字节数。
  - Fix：后续在保持透明度与细线质量的前提下评估 WebP／无损量化，并用同视口截图比较后再替换，不接受肉眼可见降质。

## 五项视觉表面复核

- 字体与排版：宋体展示标题与无衬线正文形成明确层级；当前问题在 916 与 390 下均不截断，计数、标签和正文的字重差异清楚。小字仍保持足够行高，没有把风格说明写进产品界面。
- 间距与布局节奏：916 桌面三栏比例约 190 / 自适应 / 225，主回答区拥有最大面积；390 把控制带压为 92px 并把会话占满余高。面板只用 5px 圆角、细边框和低强度阴影，没有卡片堆叠。三档 `documentElement.scrollHeight <= innerHeight + 1`，无页面级溢出。
- 色彩与 Token：实现保留源图的暖白、深海军蓝和单一钴蓝强调；网格、纸纹与边框透明度退到信息之后。按钮蓝与焦点态仍是唯一高饱和动作色。
- 图像质量与资产：五种状态分别使用真实 1254px RGBA raster 构图；没有手写 SVG、CSS 假画、emoji、截图切片、3D 球体或体积光。1440、916、390 均未出现拉伸、裁切或透明底色接缝。
- 文案与内容：主入口只保留问题探索所需的入口、三幕、当前问题、判断／风险和隐私边界；活动介绍、角色、FAQ 与 Footer 不再占据核心视口，但独立路由仍保留。

## 比较历史

### Pass 1

- P1：原纵向 Evidence Atlas 长卷与用户最新“整个 UI 不上下翻动”冲突。修正为固定视口三栏工作台，并让 `/` 与 `/start` 复用同一壳。
- P1：原单一 Coach 资产靠变换表达多状态，美术变化不足。新增 idle、listening、challenging、condensing、confirmed 五张独立高分辨率平面插画；组件按状态选择真实资产。
- P1：活动背景、角色和 FAQ 挤占主任务。它们从主入口退出并保留在既有独立路由，Footer 在工作台不可见。
- P2：390px 初版会隐藏唯一 H1。移动端改为视觉裁切但保留辅助技术可访问的标题，仍由同一真实 DOM 提供语义。

### Pass 2

- 916 × 800：同宽并排图确认平面图谱气质、蓝白比例、衬线层级与主视觉质量一致；构图按用户新要求有意从宣传长卷转为工具工作台。
- 1440 × 900：三栏稳定，当前问题、回答器与提交按钮同时可见；页面底部不超出视口。
- 390 × 844：入口、三幕和状态资产均在首屏顶部，会话与回答器占据剩余空间；没有横向或纵向页面溢出。
- 机械回归已证明：会话区 `overflow-y: auto`，Footer 不可见，状态资产自然宽度至少 1024，页面没有 `audio`、`video` 或 Coach SVG。

## Open Questions

- 正式公网环境的五张资产传输体积仍可作为 P3 性能优化，但必须以视觉无损为前提。
- 真实 GLM live 回包与人工 VoiceOver 不属于本轮视觉比较证据；不得用 fixture、Axe 或截图冒充。

## Implementation Checklist

- 固定视口与三档无溢出：完成。
- 唯一会话滚动区与固定回答器：完成。
- 五张独立状态资产与无旁白：完成。
- 活动背景退出主入口、独立路由保留：完成。
- 全量 lint / typecheck / unit / build / Mock Playwright：完成（18 files / 177 unit；51 routes；39/39 E2E）。

## Follow-up Polish

- 仅在同视口 A/B 证明确实无肉眼损失时压缩状态资产。
- 继续观察真实长回答下的会话滚动与移动软键盘，不以桌面截图替代设备手感。

final result: passed
