# Evidence Atlas 视觉对照审查

## 对照输入

- 选定视觉真值：`/Users/Zhuanz/.codex/generated_images/01a00bc8-f169-7bc0-910e-649894d3960d/exec-e02a8a10-e8a8-4715-8771-01a8d4244023.png`，916 × 1716。
- 浏览器实现全页：`docs/screenshots/flat-atlas/home-full-916.png`，916 × 6694；同宽、页面顶部、默认状态。
- 并排审查图：`docs/screenshots/flat-atlas/qa-source-vs-implementation-top.png`，1832 × 1716。左侧为视觉真值，右侧为同宽实现顶部裁切；未对任一侧做非等比缩放。
- 桌面全页：`docs/screenshots/flat-atlas/home-full-1440.png`，1440 × 6588。
- 实机浏览器：Codex 内置浏览器，1280 × 720，首页默认状态；主 CTA 已实际进入 `/start` Coach 流程并返回首页。

## 必须保留的设计特征

- 连续向下阅读的“证据图谱长卷”，首屏下沿明确露出 `01` 章节，而非一屏一页的横向幻灯片。
- 暖白纸张、深海军蓝与克制钴蓝；宋体展示标题、无衬线正文；章节编号、细竖线与工程图底纹建立编辑感。
- 主视觉是高分辨率平面资产，不是 3D 球体、体积光、CSS 图形或截图拼页。
- Coach 标记只通过平面图层偏移、套色与轻微漂移表达 idle / listening / challenging / condensing / confirmed；不使用旁白、音频、视频或“说话”动画。
- 产品主线仍为真实问题 → 主张—证据—缺口 → 三幕 Coach → 角色与边界 → 问题种子，所有原有入口可工作。

## 审查历史

### Pass 1

- P1：原首屏容易被读成正好一屏的横向展示页。修正为 6 章 + FAQ 的连续长卷；桌面 Hero 收紧至内容需要的高度，在 1280 × 720 下直接露出下一章编号。
- P1：原核心视觉依赖 3D 光核且美术密度不足。替换为四张真实平面资产：纸张底纹、Coach 地形场、证据靶心、问题种子，并记录来源与用途。
- P1：页面缺少参考图的编辑结构。补入章节轨道、工程图纸底纹、纸页式证据与终局模块，同时保留真实 DOM 与原交互。
- P2：首轮 Axe 检测发现次级文本与钴蓝小字对比度不足。加深 `--text-secondary` 与 Coach 强调色；复测无 Axe 违规。
- P2：全页截图末段问题种子因懒加载未进入捕获。终局主资产改为主动加载并复拍，资产完整出现。

### Pass 2

- 916px 真值宽度：无横向溢出；首屏比例、标题层级、钴蓝平面资产、章节轨道与纸张密度方向一致。
- 1440px 桌面：全页连续滚动，Hero 不超过一屏，下一章节可见，三张主题资产均以自然分辨率加载。
- 1024 × 768 与 390 × 844：由 Hub 响应式与无障碍 E2E 覆盖；移动端转为单列，章节轨道保留且无横向溢出。
- 主入口、Coach 三幕、移动抽屉、键盘焦点、reduced-motion 与 Axe 扫描继续通过；没有音频、视频、3D SVG 或旁白层。
- 未发现剩余可执行的 P0、P1 或 P2 视觉问题。

## 有意保留的差异

- 未复制参考图中的飞机、虚构 Logo、便笺英文或示例文字；它们不是已授权品牌事实。
- 参考图的撕纸边缘收敛为干净的编辑分隔线，避免用 CSS 假造资产或引入额外装饰成本。
- 实现保留导航、角色入口、平台边界、FAQ 与真实 Coach 表单，因此全页长于视觉概念图；这是产品功能边界，不是视觉偏差。

final result: passed
